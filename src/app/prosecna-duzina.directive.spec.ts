import { ProsecnaDuzinaDirective } from './prosecna-duzina.directive';

describe('ProsecnaDuzinaDirective', () => {
  it('should create an instance', () => {
    const directive = new ProsecnaDuzinaDirective();
    expect(directive).toBeTruthy();
  });
});

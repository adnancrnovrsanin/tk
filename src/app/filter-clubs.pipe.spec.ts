import { FilterClubsPipe } from './filter-clubs.pipe';

describe('FilterClubsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClubsPipe();
    expect(pipe).toBeTruthy();
  });
});
